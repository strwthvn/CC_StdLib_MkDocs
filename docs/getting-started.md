# Начало работы с CC_StdLib

Это руководство поможет вам начать работу с библиотекой CC_StdLib в среде разработки CODESYS 3.5.

## Установка библиотеки

### Шаг 1: Добавление библиотеки в проект
1. Откройте ваш проект в **CODESYS 3.5**
2. В дереве проекта щелкните правой кнопкой мыши на **Библиотеки**
3. Выберите **Добавить библиотеку...**
4. Найдите и выберите файл библиотеки **CC_StdLib.library**
5. Нажмите **Открыть**

### Шаг 2: Проверка установки
После установки в разделе **Библиотеки** должна появиться **CC_StdLib** со всеми функциональными блоками.

## Базовое использование

### Объявление функциональных блоков

```pascal
PROGRAM PLC_PRG
VAR
    // Дискретный сигнал с обратной связью
    signalPump : FB_SignalWithFeedback;
    
    // Аналоговый сигнал 4-20мА
    tempSensor : FB_AnalogSignal4_20mA;
    
    // Механизм с диагностикой
    motor : FB_MechanismWithFeedback;
    
    // Переменные для работы
    startButton : BOOL;
    pumpFeedback : BOOL;
    rawTemperature : REAL;
    processedTemp : REAL;
END_VAR
```

### Инициализация и вызов блоков

```pascal
// В теле программы PLC_PRG
signalPump(
    ixSignal := startButton,
    ixFeedback := pumpFeedback
);

tempSensor(
    irRawValue := rawTemperature,
    rScaleMin := 0.0,
    rScaleMax := 100.0
);

motor(
    ixFeedback := pumpFeedback
);

// Получение обработанных значений
processedTemp := tempSensor.GetScaledValue();
```

## Структура проекта CODESYS 3.5

### Рекомендуемая организация

```
Проект CODESYS
├── Библиотеки
│   └── CC_StdLib
├── POUs (Program Organization Units)
│   ├── MAIN (PROGRAM)
│   ├── Signals (FOLDER)
│   │   ├── AnalogInputs (PROGRAM) 
│   │   └── DigitalInputs (PROGRAM)
│   ├── Controls (FOLDER)
│   │   ├── Motors (PROGRAM)
│   │   └── Valves (PROGRAM)
│   └── Communications (FOLDER)
└── Глобальные переменные
    ├── GVL_Inputs
    ├── GVL_Outputs
    └── GVL_Internal
```

### Настройка задач (Tasks)

1. В **Конфигураторе задач** создайте задачу с периодом **10ms** для критичных сигналов
2. Создайте задачу с периодом **100ms** для общего управления
3. Назначьте соответствующие программы задачам

## Практические примеры

### Пример 1: Управление насосом с обратной связью

```pascal
PROGRAM PumpControl
VAR
    pumpSignal : FB_SignalWithFeedbackTimer;
    startCmd : BOOL;
    pumpRunning : BOOL;
    pumpError : BOOL;
END_VAR

pumpSignal(
    ixSignal := startCmd,
    ixFeedback := pumpRunning,
    tFeedbackTimeout := T#5S
);

// Проверка ошибок
pumpError := pumpSignal.GetFeedbackTimeout();
```

### Пример 2: Обработка аналогового датчика температуры

```pascal
PROGRAM TemperatureProcessing
VAR
    tempInput : FB_UniversalAnalogSignal;
    diagnostic : FB_RangeDiagnostic_LH;
    rawValue : REAL;
    temperature : REAL;
    tempAlarm : BOOL;
END_VAR

tempInput(
    irRawValue := rawValue,
    rInputMin := 4.0,    // 4мА
    rInputMax := 20.0,   // 20мА
    rScaleMin := 0.0,    // 0°C
    rScaleMax := 150.0   // 150°C
);

diagnostic(
    irValue := tempInput.GetScaledValue(),
    irSetpointL := 10.0,   // Низкое предупреждение
    irSetpointH := 130.0,  // Высокое предупреждение
    irSetpointLL := 5.0,   // Низкая авария
    irSetpointHH := 140.0, // Высокая авария
    ixEnable := TRUE
);

temperature := tempInput.GetScaledValue();
tempAlarm := diagnostic.qxAlarmActive;
```

## Конфигурация CODESYS 3.5

### Настройки компилятора
- Установите **IEC 61131-3 Standard** в настройках проекта
- Включите **Support for object-oriented programming**
- Установите **Warning level** на **All warnings**

### Отладка
- Используйте **Watch списки** для мониторинга состояния функциональных блоков
- Настройте **Trace** для записи значений аналоговых сигналов
- Применяйте **Breakpoints** для пошаговой отладки логики

## Следующие шаги

- Изучите [Архитектуру](diagram.md) для понимания взаимосвязей между блоками
- Ознакомьтесь с разделами по [Сигналам] и [Механизмам] для детального изучения
- Проверьте раздел [Коммуникация](Communication.md) для работы с протоколами связи
- Экспериментируйте с различными функциональными блоками в тестовых проектах

## Полезные советы

💡 **Совет**: Всегда инициализируйте функциональные блоки в секции VAR с начальными значениями

💡 **Совет**: Используйте префиксы для именования (например, `sig_`, `motor_`, `valve_`) для лучшей организации кода

💡 **Совет**: Регулярно вызывайте методы `.Reset()` при необходимости сброса состояния блоков